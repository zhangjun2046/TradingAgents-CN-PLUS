# 🔧 立即修复管理员账户 - 2分钟解决

## 🎯 问题原因

您之前使用了 **bcrypt 密码格式**，但系统使用的是 **SHA-256 格式**。

```diff
- 错误的密码哈希: "$2b$12$LQv3c1yqBWcVAqaKKyHqhO5XQXM1zzqLZJXNZLEAP8pSJm8Y1bF4K"  ❌ bcrypt
+ 正确的密码哈希: "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9"  ✅ SHA-256
```

---

## ✅ MVP 操作清单

### □ 第1步：删除旧文档（30秒）

**在您当前的 MongoDB Atlas 页面**：

1. 找到已存在的 admin 用户文档（截图中可见）
2. 将鼠标悬停在文档上
3. 点击文档右侧的 **"..."** 菜单或 **🗑️ 垃圾桶图标**
4. 选择 **"Delete Document"**
5. 确认删除

✅ 结果：users 集合应该为空（0 documents）

---

### □ 第2步：插入正确文档（30秒）

**在同一页面**：

1. 点击绿色按钮 **"ADD DATA"** → **"Insert Document"**
2. 在弹出窗口中，切换到 **"{ } JSON"** 视图（右上角）
3. **打开项目中的 `admin_user.json` 文件**
4. **复制全部内容**（Ctrl+A → Ctrl+C）
5. **粘贴**到 MongoDB 编辑器中（覆盖默认内容）
6. 点击 **"Insert"** 按钮

✅ 结果：users 集合应该有 1 个文档

---

### □ 第3步：测试登录（10秒）

1. 访问：https://tradingagents-cn-plus.onrender.com/login
2. 输入凭证：
   ```
   用户名: admin
   密码: admin123
   ```
3. 点击 **"登录"**

✅ 结果：应该成功进入系统！

---

### □ 第4步：修改密码（必须！）

1. 登录后进入个人设置
2. 修改默认密码为强密码
3. 保存并重新登录验证

---

## 📋 正确的 JSON 文件

**文件位置**: `admin_user.json`（已更新为正确格式）

**关键字段对比**:

```json
{
  "username": "admin",
  "hashed_password": "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9",
  ☝️ 这是 SHA-256 格式，对应密码 admin123
  
  "is_active": true,
  "is_admin": true,
  ...其他字段
}
```

---

## 🔍 验证要点

插入前检查 JSON：

- [ ] `hashed_password` 是 **64位十六进制字符串**（不是 `$2b$12$...` 开头）
- [ ] `hashed_password` = `240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9`
- [ ] `is_active` = `true`
- [ ] `is_admin` = `true`
- [ ] `username` = `admin`

---

## ⚠️ 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| 登录失败 | 密码格式错误（bcrypt vs SHA-256） | 使用正确的 JSON |
| 文档已存在 | 旧文档未删除 | 先删除旧文档 |
| 格式错误 | 手动输入有误 | 直接复制 admin_user.json |

---

## 🎉 预期结果

完成后您应该能：
- ✅ 使用 `admin` / `admin123` 成功登录
- ✅ 看到系统主界面
- ✅ 访问所有管理功能

---

**预计总耗时**: 2 分钟  
**操作步骤**: 4 步  
**成功率**: 99.9%

开始操作吧！ 🚀

