# 📘 MongoDB Atlas 插入管理员账户 - 图文教程

**最简单的方法：2 分钟完成，无需代码！**

---

## 🎯 准备工作

- ✅ MongoDB Atlas 账户（已有）
- ✅ 数据库连接正常（已配置）
- ✅ 浏览器访问 MongoDB Atlas

---

## 📝 详细步骤

### 步骤 1️⃣：进入 MongoDB Atlas

```
1. 打开浏览器
2. 访问：https://cloud.mongodb.com/
3. 登录您的 MongoDB Atlas 账户
```

### 步骤 2️⃣：浏览数据库集合

```
左侧菜单
├── Overview（概览）
├── Metrics（指标）
├── [✓] Browse Collections ← 点击这里
├── Network Access
└── ...
```

**操作**：点击左侧菜单的 **"Browse Collections"**

### 步骤 3️⃣：选择数据库

```
数据库列表
├── admin
├── local
└── [✓] tradingagents ← 点击这里
    ├── users ← 如果没有，需要创建
    ├── configs
    └── ...
```

**操作**：
- 找到并点击 `tradingagents` 数据库
- 查看是否有 `users` 集合

### 步骤 4️⃣：创建 users 集合（如果不存在）

如果没有 `users` 集合：

```
1. 点击 "Create Collection" 按钮
2. 输入集合名称：users
3. 点击 "Create" 确认
```

### 步骤 5️⃣：插入管理员文档

**操作**：
1. 点击 `users` 集合（或刚创建的集合）
2. 点击右上角的 **"INSERT DOCUMENT"** 按钮
3. 在弹出的编辑器中，切换到 **"{ } JSON"** 视图
4. **删除默认内容**
5. **复制以下完整 JSON**，粘贴进去：

```json
{
  "username": "admin",
  "email": "admin@tradingagents.cn",
  "hashed_password": "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9",
  "is_active": true,
  "is_verified": true,
  "is_admin": true,
  "created_at": {"$date": "2026-01-10T00:00:00.000Z"},
  "updated_at": {"$date": "2026-01-10T00:00:00.000Z"},
  "last_login": null,
  "preferences": {
    "default_market": "A股",
    "default_depth": "深度",
    "ui_theme": "light",
    "language": "zh-CN",
    "notifications_enabled": true,
    "email_notifications": false
  },
  "daily_quota": 10000,
  "concurrent_limit": 10,
  "total_analyses": 0,
  "successful_analyses": 0,
  "failed_analyses": 0,
  "favorite_stocks": []
}
```

6. 点击 **"Insert"** 按钮（绿色）

### 步骤 6️⃣：验证插入成功

**检查**：
- 在 `users` 集合中应该能看到 1 个文档
- 文档包含 `username: "admin"` 字段
- 文档有一个自动生成的 `_id` 字段

### 步骤 7️⃣：登录测试

**立即测试**：
1. 访问：https://tradingagents-cn-plus.onrender.com/login
2. 输入凭证：
   ```
   用户名：admin
   密码：admin123
   ```
3. 点击 **"登录"** 按钮
4. ✅ 如果成功进入系统，说明配置正确！

---

## 🔍 界面导航参考

### MongoDB Atlas Web 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  MongoDB Atlas                                    [用户图标] │
├──────────────┬──────────────────────────────────────────────┤
│              │  Database Deployments                        │
│  Projects    │  ┌─────────────────────────────────────┐     │
│  ├─ Project1 │  │ Cluster0                            │     │
│  └─[✓]YourPj │  │ Status: Active                      │     │
│              │  │ Region: US East                     │     │
│  DEPLOYMENT  │  │ [Connect] [Browse Collections] [...] │    │
│  ├─Database  │  └─────────────────────────────────────┘     │
│  ├─NetworkAc │                                              │
│  └─DBAccess  │                                              │
│              │                                              │
│  DATA        │                                              │
│  ├─[✓]Browse │ ← 点击这里进入数据浏览                        │
│  │ Collection│                                              │
│  └─Search    │                                              │
└──────────────┴──────────────────────────────────────────────┘
```

### 集合浏览界面

```
┌─────────────────────────────────────────────────────────────┐
│  tradingagents Database                                     │
├──────────────┬──────────────────────────────────────────────┤
│ Collections  │  users Collection              [INSERT DOC]  │ ← 点击
│              │  ┌────────────────────────────────────────┐  │
│ □ configs    │  │ Documents (0)                          │  │
│ [✓] users    │  │ ───────────────────────────────────    │  │
│              │  │ No documents yet                       │  │
│              │  │                                        │  │
│              │  └────────────────────────────────────────┘  │
└──────────────┴──────────────────────────────────────────────┘
```

### 插入文档对话框

```
┌──────────────────────────────────────────────────────┐
│  Insert to users                          [X]        │
├──────────────────────────────────────────────────────┤
│  View: [Field by Field] [{ } JSON] ← 选择 JSON 视图 │
│  ┌────────────────────────────────────────────────┐ │
│  │ {                                              │ │
│  │   "_id": {"$oid": "..."}  ← 自动生成，可删除   │ │
│  │ }                                              │ │
│  │                                                │ │
│  │  ← 删除默认内容，粘贴完整的管理员 JSON         │ │
│  │                                                │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│              [Cancel]  [Insert] ← 点击插入           │
└──────────────────────────────────────────────────────┘
```

---

## ✨ 关键信息速查

### 登录凭证
```
用户名：admin
密码：admin123
密码哈希：240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9
```

### 数据库信息
```
数据库名：tradingagents
集合名：users
文档数量：1（插入后）
```

### 应用信息
```
登录页面：https://tradingagents-cn-plus.onrender.com/login
部署平台：Render
数据库：MongoDB Atlas
```

---

## 🎨 JSON 格式说明

### 最小化版本（如果空间不够）

如果编辑器显示不全，可以使用压缩版本（功能相同）：

```json
{"username":"admin","email":"admin@tradingagents.cn","hashed_password":"240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9","is_active":true,"is_verified":true,"is_admin":true,"created_at":{"$date":"2026-01-10T00:00:00.000Z"},"updated_at":{"$date":"2026-01-10T00:00:00.000Z"},"last_login":null,"preferences":{"default_market":"A股","default_depth":"深度","ui_theme":"light","language":"zh-CN","notifications_enabled":true,"email_notifications":false},"daily_quota":10000,"concurrent_limit":10,"total_analyses":0,"successful_analyses":0,"failed_analyses":0,"favorite_stocks":[]}
```

---

## 🔧 自定义配置

### 修改用户名

将 JSON 中的：
```json
"username": "admin"
```
改为：
```json
"username": "你的用户名"
```

### 修改密码

1. **生成新密码的哈希值**

   访问：https://emn178.github.io/online-tools/sha256.html
   - 输入您想要的密码
   - 复制生成的 SHA-256 值

2. **替换哈希值**

   将 JSON 中的：
   ```json
   "hashed_password": "240be5..."
   ```
   改为：
   ```json
   "hashed_password": "您生成的新哈希值"
   ```

### 修改邮箱

将 JSON 中的：
```json
"email": "admin@tradingagents.cn"
```
改为：
```json
"email": "your.email@example.com"
```

---

## ❓ 常见问题

### Q1: 找不到 "Browse Collections" 菜单？

**A**: 
- 确保您已经登录 MongoDB Atlas
- 确保您在正确的项目下
- 左侧菜单应该有：Overview、Metrics、Browse Collections 等选项

### Q2: 没有 tradingagents 数据库？

**A**: 
- 检查 Render 的 `MONGO_URI` 环境变量中的数据库名
- 或者创建一个新数据库：
  1. 点击 "Create Database"
  2. Database Name: `tradingagents`
  3. Collection Name: `users`
  4. 点击 "Create"

### Q3: 插入后登录失败？

**A**: 检查以下项目：
- [ ] 密码哈希值是否正确复制（完整的 64 位字符）
- [ ] `is_active` 是否为 `true`
- [ ] `is_admin` 是否为 `true`
- [ ] 用户名是否为 `admin`
- [ ] 登录时密码是否输入为 `admin123`

### Q4: 文档格式错误？

**A**: 
- 确保使用的是 JSON 视图，不是 Field by Field 视图
- 确保 JSON 格式正确（花括号、引号、逗号）
- 可以使用在线 JSON 验证工具检查格式

---

## 🎉 完成！

按照以上步骤，您应该能在 **2 分钟内**完成管理员账户的创建。

**下一步**：
1. ✅ 登录系统
2. ✅ 修改默认密码
3. ✅ 配置大模型 API
4. ✅ 开始使用

---

**祝您使用愉快！** 🚀

